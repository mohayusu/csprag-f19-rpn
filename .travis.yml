language: python
python:
  - "3.5"
install: 
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
script:
  - python -m unittest test_rpn --cov=test_rpn/
after_success:
  - coveralls

